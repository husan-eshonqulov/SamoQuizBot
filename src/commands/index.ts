import Command from '../types/command';

const commandList: Command[] = [
  { command: 'start', description: 'restart bot' },
  { command: 'help', description: 'take help' },
];

export default commandList;
